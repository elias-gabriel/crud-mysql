import mysql.connector
from mysql.connector import Error

con = mysql.connector.connect(host='localhost', user='', password='', database='crud',
                                          charset='utf8')
